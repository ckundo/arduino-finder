<!DOCTYPE html>
<meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
<head>
  <style type="text/css">
    html {
      height:100%;
      width:100%;
    }
    
    body {
      margin:0;
      height:100%;
    }

    div#map {
      position: relative;
    }

    div#crosshair {
      position: absolute;
      top: 192px;
      height: 19px;
      width: 19px;
      left: 50%;
      margin-left: -8px;
      display: block;
      background: url(crosshair.gif);
      background-position: center center;
      background-repeat: no-repeat;
    }
  </style>
  <script type="text/javascript" src="http://maps.google.com/maps/api/js?key=AIzaSyAVn00VcAGPHCl9bYF8wb16_m731_isNwA&sensor=false"></script>
  <%= javascript_include_tag :application %>
</head>
<body>
  <%= yield %>
  <script type="text/javascript">
    store = new Store();
    store.fetchLocations(function(locations) {
      console.log(locations);
      mapper = new Mapper(locations);
      mapper.renderMap();
    });
  </script>
</body>
</html>
